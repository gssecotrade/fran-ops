name: Fran Ops — Scheduler

on:
  workflow_dispatch:        # Lanzamiento manual
  schedule:
    - cron: '0 6 * * *'     # 08:00 Madrid (cron usa UTC)

jobs:
  run-ops:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      # Restaura Service Account JSON (por si lo requieren otros scripts)
      - name: Restore Service Account JSON
        env:
          GOOGLE_SA_JSON_BASE64: ${{ secrets.GOOGLE_SA_JSON_BASE64 }}
        run: |
          mkdir -p ops/.secrets
          echo "$GOOGLE_SA_JSON_BASE64" | base64 -d > ops/.secrets/google-sa.json

      # .env (opcional para otros scripts)
      - name: Create .env
        run: |
          cat > ops/.env <<'ENV'
          GOOGLE_SA_JSON=${{ github.workspace }}/ops/.secrets/google-sa.json
          SHEETS_SPREADSHEET_ID=${{ secrets.SHEETS_SPREADSHEET_ID }}
          GDRIVE_FOLDER_ID=${{ secrets.GDRIVE_FOLDER_ID }}

          # OAuth Drive
          GDRIVE_OAUTH_CLIENT_ID=${{ secrets.GDRIVE_OAUTH_CLIENT_ID }}
          GDRIVE_OAUTH_CLIENT_SECRET=${{ secrets.GDRIVE_OAUTH_CLIENT_SECRET }}
          GDRIVE_OAUTH_REFRESH_TOKEN=${{ secrets.GDRIVE_OAUTH_REFRESH_TOKEN }}

          SUMMARY_EMAIL=${{ secrets.SUMMARY_EMAIL }}
          SMTP_HOST=${{ secrets.SMTP_HOST }}
          SMTP_PORT=${{ secrets.SMTP_PORT }}
          SMTP_USER=${{ secrets.SMTP_USER }}
          SMTP_PASS=${{ secrets.SMTP_PASS }}
          SMTP_FROM=${{ secrets.SMTP_FROM }}
          SMTP_TO=${{ secrets.SMTP_TO }}
          ENV

      - name: Install deps
        run: |
          pip install -r ops/scripts/requirements.txt || true
          pip install google-api-python-client google-auth-httplib2 google-auth-oauthlib pandas pytz tzdata

      # Debug rápido de credenciales y carpeta
      - name: Debug Drive OAuth & Folder
        env:
          GDRIVE_OAUTH_CLIENT_ID:     ${{ secrets.GDRIVE_OAUTH_CLIENT_ID }}
          GDRIVE_OAUTH_CLIENT_SECRET: ${{ secrets.GDRIVE_OAUTH_CLIENT_SECRET }}
          GDRIVE_OAUTH_REFRESH_TOKEN: ${{ secrets.GDRIVE_OAUTH_REFRESH_TOKEN }}
          GDRIVE_FOLDER_ID:           ${{ secrets.GDRIVE_FOLDER_ID }}
        run: |
          python - <<'PY'
          import os
          from google.oauth2.credentials import Credentials
          from googleapiclient.discovery import build

          assert os.environ.get("GDRIVE_OAUTH_CLIENT_ID")
          assert os.environ.get("GDRIVE_OAUTH_CLIENT_SECRET")
          assert os.environ.get("GDRIVE_OAUTH_REFRESH_TOKEN")
          assert os.environ.get("GDRIVE_FOLDER_ID")

          creds = Credentials(
              None,
              refresh_token=os.environ["GDRIVE_OAUTH_REFRESH_TOKEN"],
              client_id=os.environ["GDRIVE_OAUTH_CLIENT_ID"],
              client_secret=os.environ["GDRIVE_OAUTH_CLIENT_SECRET"],
          )
          svc = build("drive","v3",credentials=creds)

          about = svc.about().get(fields="user(emailAddress)").execute()
          print("AUTH_USER:", about["user"]["emailAddress"])

          fid = os.environ["GDRIVE_FOLDER_ID"].strip()
          meta = svc.files().get(
              fileId=fid, fields="id,name,mimeType,parents", supportsAllDrives=True
          ).execute()
          print("FOLDER_OK:", meta["name"], meta["id"])
          PY

      # ===== Pipeline =====
      - name: Run pipeline (Sheets -> CSV -> Normalize -> ZIP -> Drive -> Rotate -> Email)
        env:
          # Email
          SMTP_HOST: ${{ secrets.SMTP_HOST }}
          SMTP_PORT: ${{ secrets.SMTP_PORT }}
          SMTP_USER: ${{ secrets.SMTP_USER }}
          SMTP_PASS: ${{ secrets.SMTP_PASS }}
          SMTP_FROM: ${{ secrets.SMTP_FROM }}
          SMTP_TO:   ${{ secrets.SMTP_TO }}
          SUMMARY_EMAIL: ${{ secrets.SUMMARY_EMAIL }}

          # IDs y rutas
          SHEETS_SPREADSHEET_ID: ${{ secrets.SHEETS_SPREADSHEET_ID }}
          GDRIVE_FOLDER_ID:      ${{ secrets.GDRIVE_FOLDER_ID }}
          GOOGLE_SA_JSON:        ${{ github.workspace }}/ops/.secrets/google-sa.json

          # OAuth Drive
          GDRIVE_OAUTH_CLIENT_ID:     ${{ secrets.GDRIVE_OAUTH_CLIENT_ID }}
          GDRIVE_OAUTH_CLIENT_SECRET: ${{ secrets.GDRIVE_OAUTH_CLIENT_SECRET }}
          GDRIVE_OAUTH_REFRESH_TOKEN: ${{ secrets.GDRIVE_OAUTH_REFRESH_TOKEN }}

          # Rotación
          RETENTION_DAYS: "14"
          ROTATE_DRY_RUN: "0"
        run: |
          set -e
          python ops/scripts/sheets_to_csv.py
          python ops/scripts/normalize_loterias.py
          bash ops/scripts/zip_all.sh
          python ops/scripts/upload_to_gdrive.py
          python ops/scripts/rotate_drive.py
          python ops/scripts/send_summary_email.py
